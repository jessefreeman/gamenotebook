export type ThemeMode = "system" | "light" | "dark"
export type UiPaletteMode = "default" | "muted" | "high-contrast"
export type PlayerPaletteMode = "default" | "editor"

export const THEME_MODES: ThemeMode[] = ["system", "light", "dark"]
export const UI_PALETTE_MODES: UiPaletteMode[] = [
  "default",
  "muted",
  "high-contrast",
]
export const PLAYER_PALETTE_MODES: PlayerPaletteMode[] = ["default", "editor"]
